-- 7. Inserção em Lote
INSERT INTO Alunos (nome, email, data_nascimento) VALUES
('Ana Silva', 'ana@email.com', '2001-05-15'),
('Carlos Souza', 'carlos@email.com', '1998-10-20'),
('Beatriz Costa', 'beatriz@email.com', '2003-02-01'),
('Arthur Mendes', 'arthur@email.com', '2000-11-12'),
('Fernanda Lima', 'fernanda@email.com', '1995-07-30');

INSERT INTO Cursos (nome, carga_horaria) VALUES
('SQL para Análise de Dados', 40),
('Desenvolvimento Java', 80),
('Estruturas de Dados', 60);

-- 8. Filtros Condicionais
SELECT * FROM Produtos 
WHERE preco BETWEEN 50.00 AND 200.00 
  AND status = 'ativo';

-- 9. Busca por Padrões
SELECT * FROM Alunos 
WHERE nome LIKE 'A%' OR nome LIKE '%Silva%';

-- 10. Tratamento de Valoração Nula
SELECT 
    nome, 
    COALESCE(telefone, 'Não informado') AS telefone_contato
FROM Alunos;

-- 11. Ordenação e Paginação
SELECT * FROM Produtos 
ORDER BY preco DESC 
LIMIT 5 OFFSET 0;

-- 12. Atualização Condicional
UPDATE Produtos 
SET preco = preco * 1.10 
WHERE status = 'ativo';
